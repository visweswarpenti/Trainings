#include <stdio.h>
int main()
{
    short signed int length;
    float breadth;
    float perimeter;
    length = 3;
    breadth = 4;
    perimeter = 2 * (length + breadth);
    printf("%f", perimeter);

    return 0;
}
